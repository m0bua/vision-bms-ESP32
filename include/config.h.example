#pragma once

#include <stdint.h>

// Local settings for this project.
// Put real Wi-Fi credentials here before flashing.

constexpr const char* WIFI_SSID = "your-wifi-ssid";
constexpr const char* WIFI_PASSWORD = "your-wifi-password";

constexpr bool ENABLE_DEYE_CAN = true;

constexpr uint8_t BMS_DE_PIN = 4;
constexpr uint8_t BMS_RX_PIN = 16;
constexpr uint8_t BMS_TX_PIN = 17;

constexpr uint8_t CAN_RX_PIN = 18;
constexpr uint8_t CAN_TX_PIN = 19;

constexpr uint8_t PREFERRED_BMS_ID = 16;
constexpr uint8_t BMS_ID_MIN = 1;
constexpr uint8_t BMS_ID_MAX = 16;
